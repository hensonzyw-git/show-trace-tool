# Secrets
.env

# Local data — raw HTML, SQLite, generated digests, launchd log
# (fixtures 保留在版本控制里，是项目示例数据)
data/raw/
data/digests/
data/events.db
data/launchd.log

# Claude Code session data
.claude/

# 持久化浏览器 profile（含 cookie / 缓存，本地养出来，不进版本控制）
.browser-profile/

# Python
venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/

# OS / Editor
.DS_Store
.vscode/
.idea/
