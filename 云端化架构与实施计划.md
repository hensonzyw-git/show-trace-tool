# 云端化架构与实施计划

> 目标：把当前可用的本地演出活动监控工具，演进成一个单人使用、服务端优先、iOS 客户端后置的完整作品级 demo。
>
> 本文档是下一阶段长期 coding 的工作底稿：先稳定服务端链路，再逐步接入困难信息源，最后补 iOS 客户端和作品展示材料。

---

## 1. 第一性原理拆解

这个项目的本质不是“写一个爬虫”，而是：

```
不稳定信息源 -> 可复用采集 -> 结构化抽取 -> 去重存储 -> 订阅筛选 -> 每日摘要 -> 用户查看
```

从这个底层流程看，真正重要的是四件事：

1. **采集不稳定，但后续管道必须稳定**
   - 大麦、小红书、微博等平台会有登录态、风控、页面改版、验证码。
   - 因此采集层必须允许多种实现：HTTP API、浏览器自动化、Computer Use、人工辅助。
   - 采集失败不应该拖垮抽取、存储、摘要和客户端。

2. **单人使用，不需要多租户复杂度**
   - 不需要复杂账号体系。
   - 不需要权限模型。
   - 订阅配置可以先作为一份服务端配置或数据库记录维护。
   - iOS 只需要开发者安装，不需要 App Store 发布链路。

3. **当前是每日批处理，不做实时系统**
   - 每天跑一次符合业务需求。
   - 不做 WebSocket、实时推送、分钟级轮询。
   - “手动触发一次采集”可以作为 demo 功能，但不改变主流程。

4. **作为求职作品，展示工程判断比堆功能更重要**
   - 要展示从本地脚本到服务端系统的演进。
   - 要展示对反爬和困难信息源的现实处理方式。
   - 要展示清晰 API、数据模型、调度、失败隔离和可观测性。

---

## 2. 当前状态

当前项目已经具备本地 MVP：

- 多平台采集：
  - 大麦：`patchright + Chrome profile`
  - 秀动：`requests + SSR HTML`
  - 摩天轮：`requests + JSON API`
- LLM 抽取：DeepSeek OpenAI-compatible API
- 存储：SQLite `events` 表去重
- 输出：
  - Markdown digest
  - macOS 通知
  - 飞书 webhook / 飞书自建应用
- 调度：macOS `launchd` 每天 10:00 跑一次

已有管道的价值在于：它证明了 `Source -> Extractor -> DB -> Notifier` 的主链路可行。

下一阶段不要推翻它，而是把它拆成可上云的服务端组件。

---

## 3. 目标架构

### 3.1 总体形态

```
                          +----------------------+
                          |      iOS App         |
                          |  今日摘要 / 订阅设置  |
                          +----------+-----------+
                                     |
                                     | HTTPS JSON API
                                     |
+----------------------+   +---------v----------+   +----------------------+
| Scheduled Worker     |   | Backend API        |   | Database             |
| 每日采集 / 抽取 / 入库 |-->| FastAPI            |-->| SQLite -> Postgres   |
+----------+-----------+   | 摘要 / 事件 / 订阅  |   +----------------------+
           |               +---------+----------+
           |                         ^
           |                         |
+----------v-----------+             |
| Source Adapters      |             |
| Http / Browser /     |-------------+
| Assisted             |  raw/events submit API
+----------------------+
```

### 3.2 核心原则

1. **先服务端，后 iOS**
   - 先让后端有稳定 API。
   - iOS 只是 API 的消费端，不承载采集逻辑。

2. **先保留 SQLite，后切 Postgres**
   - 单人 demo 初期 SQLite 足够。
   - 上云时如果平台对持久磁盘支持弱，再切 Postgres。
   - 先把存储访问封装好，避免业务代码散落 SQL。

3. **稳定源云端跑，困难源允许本地辅助**
   - 秀动、摩天轮这类 HTTP/API 源优先上云。
   - 大麦、小红书、微博这类困难源先做 Browser/Assisted adapter。
   - 如果云服务器跑浏览器不稳定，本机 worker 也可以把结果提交到云端 API。

4. **每日批处理不变**
   - 默认每天固定时间跑。
   - API 提供手动触发作为 demo / debug 能力。
   - 不设计实时推送和高频监控。

---

## 4. 服务端模块设计

### 4.1 Backend API

建议使用 FastAPI，原因：

- 与当前 Python 项目自然衔接。
- 自动生成 OpenAPI 文档，适合作品展示。
- 易于给 iOS 提供 JSON API。
- 也方便后续加一个简单 Web 管理页。

首批 API：

| 方法 | 路径 | 用途 |
|---|---|---|
| `GET` | `/health` | 健康检查 |
| `GET` | `/api/digests/today` | 获取今日摘要 |
| `GET` | `/api/digests` | 获取历史摘要列表 |
| `GET` | `/api/events` | 获取事件列表，支持日期 / 城市 / 类型筛选 |
| `GET` | `/api/subscriptions` | 获取订阅配置 |
| `PUT` | `/api/subscriptions` | 修改艺人、城市、关键词、启用源 |
| `POST` | `/api/runs` | 手动触发一次采集 |
| `GET` | `/api/runs` | 查看最近运行记录 |

单人使用下，认证可以先用一个简单的 `API_TOKEN`：

- iOS 请求带 `Authorization: Bearer <token>`
- 服务端环境变量配置 token
- 不做账号、登录、刷新 token

### 4.2 Worker

Worker 负责每日主流程：

```
load subscriptions
-> build tasks
-> run enabled sources
-> save raw captures
-> extract events
-> upsert events
-> create daily digest
-> record run status
```

原来的 `main.py` 可以逐步改造成：

- `app/pipeline.py`：纯管道逻辑
- `app/worker.py`：CLI / scheduled job 入口
- `app/api.py`：FastAPI 入口

不要让 API 直接塞满采集逻辑。API 只负责触发和查询，真正的采集仍在 pipeline。

### 4.3 Source Adapter 分层

建议把 source 分成三类：

#### HttpSource

适合：

- 秀动
- 摩天轮
- 其他无强反爬 JSON API / SSR 页面

特点：

- 可在云端稳定运行。
- 无需登录态。
- 是每日主链路的基础。

#### BrowserSource

适合：

- 大麦
- 猫眼
- 需要 JS 渲染或 cookie 的平台

特点：

- 使用 Playwright / Patchright / Chrome profile。
- 云端可尝试跑 headless Chrome，但不能假设一定稳定。
- 要有超时、失败记录和截图/HTML 留存。

#### AssistedSource

适合：

- 小红书
- 微博
- 任何强登录态、强风控、搜索结果难以直接抓的平台

特点：

- 可以使用 Computer Use 或 Chrome 插件能力搜索。
- 目标不是“完美爬虫”，而是“获取可供 LLM 抽取的原始文本/截图/页面内容”。
- 可以先半自动：人工登录 + 工具导航 + 保存搜索结果。
- 输出仍然进入统一的 raw capture 和 extractor。

---

## 5. 数据模型演进

### 5.1 保留 `events`

当前 `events` 表继续作为核心事实表。

建议后续补充字段：

| 字段 | 用途 |
|---|---|
| `start_date` | 从 `event_date` 解析出的开始日期，便于排序和筛选 |
| `end_date` | 区间活动结束日期 |
| `normalized_title` | 去重辅助 |
| `confidence` | LLM 抽取置信度或规则置信度 |
| `last_seen` | 最近一次被源看到的时间 |
| `updated_at` | 事件信息更新时间 |

### 5.2 新增 `subscriptions`

单人使用可以先只存一行 JSON 配置。

字段建议：

| 字段 | 用途 |
|---|---|
| `id` | 固定为 `default` |
| `artists` | 关注艺人数组 |
| `city` | 主城市 |
| `keywords` | 本地发现关键词 |
| `enabled_sources` | 启用的数据源 |
| `updated_at` | 修改时间 |

### 5.3 新增 `runs`

用于展示和排查每日任务。

字段建议：

| 字段 | 用途 |
|---|---|
| `id` | run id |
| `started_at` | 开始时间 |
| `finished_at` | 结束时间 |
| `status` | `success` / `partial_success` / `failed` |
| `total_raw_captures` | 原始采集数量 |
| `total_extracted_events` | 抽取事件数量 |
| `new_events` | 新增事件数量 |
| `error_summary` | 错误摘要 |

### 5.4 新增 `daily_digests`

Markdown 文件仍可保留，但 API 最好读数据库。

字段建议：

| 字段 | 用途 |
|---|---|
| `digest_date` | 摘要日期 |
| `title` | 标题 |
| `markdown` | Markdown 内容 |
| `event_count` | 事件数量 |
| `created_at` | 生成时间 |

---

## 6. 上云策略

### 6.1 推荐部署形态

第一版云端可以采用单服务：

```
FastAPI app + scheduled worker + SQLite/Postgres
```

可选平台：

- Render / Railway / Fly.io：适合快速 demo。
- VPS：更自由，适合跑浏览器自动化。
- Supabase Postgres + 任意 Python host：适合把数据库托管出去。

如果要跑浏览器自动化，VPS 或 Fly.io 这类更可控的环境更合适。

### 6.2 云端浏览器现实约束

云服务器理论上可以安装：

- Python
- Playwright / Patchright
- Chrome / Chromium
- Xvfb 或 headless 模式
- Codex CLI / 其他自动化工具

但困难源不稳定的根因通常不是“有没有浏览器”，而是：

- 云服务器 IP 更像机房流量，更容易被风控。
- 登录态维护麻烦。
- 验证码和滑块无法保证自动处理。
- 小红书等平台对自动化环境敏感。

因此设计上不要把困难源的成功率作为服务端主链路前提。

更稳的策略：

```
云端每日跑稳定源
本机/Chrome/Computer Use 辅助跑困难源
两者都通过 API 写入同一套数据
```

---

## 7. iOS App 范围

iOS 是后置流程，不影响服务端第一阶段。

### 7.1 最小功能

| 页面 | 功能 |
|---|---|
| 今日摘要 | 查看今日 digest，按最近开始的活动优先 |
| 事件列表 | 按日期、城市、类型查看事件 |
| 订阅设置 | 修改主要艺人、城市、关键词、启用源 |

### 7.2 不做的功能

- 不做实时推送。
- 不做复杂登录。
- 不做多人订阅。
- 不做购票闭环。
- 不做 App Store 发布。

### 7.3 技术建议

- SwiftUI
- `URLSession` 调 FastAPI
- 本地保存 API base URL 和 token
- 开发者安装即可

---

## 8. 实施路线

### Phase 1：服务端 API 骨架

目标：不改变采集能力，只把现有数据通过 API 暴露出来。

任务：

1. 新建 `app/` 包结构。
2. 封装数据库访问层。
3. 增加 FastAPI。
4. 实现 `/health`、`/api/events`、`/api/digests/today`。
5. 保留现有 `main.py` 可运行。

完成标准：

- 本地启动 API 后，可以从浏览器或 curl 查看今日摘要和事件列表。
- 原有每日任务仍能跑。

### Phase 2：订阅配置服务端化

目标：用 API 管理订阅配置，逐步替代手改 `config.yaml`。

完成内容（2026-06-01 已完成）：

- 新增 `subscriptions` 表。
- 首次启动时从 `config.yaml` 初始化默认订阅。
- 实现 `GET /api/subscriptions` 和 `PUT /api/subscriptions`。
- CLI worker 和 API 手动触发均从数据库读取订阅。

任务：

1. 新增 `subscriptions` 表。
2. 写 config migration：从 `config.yaml` 初始化订阅。
3. 实现 `GET/PUT /api/subscriptions`。
4. Worker 从数据库读取订阅。

完成标准：

- 修改订阅不再需要编辑文件。
- 下一次采集使用数据库里的订阅。

### Phase 3：运行记录与手动触发

目标：让系统像一个服务，而不是一次性脚本。

完成内容（2026-06-01 已完成）：

- 新增 `runs` 表。
- 每次 CLI/API worker 运行都会记录 trigger、fixture、notify、状态和事件计数。
- 实现 `POST /api/runs` 手动触发一次采集。
- 实现 `GET /api/runs` 查看最近运行记录。
- Source 级失败会进入 `error_summary`，不阻断其他 source；有成功采集且有错误时状态为 `partial_success`。

任务：

1. 新增 `runs` 表。
2. 每次 worker 记录成功/失败、事件数量、错误摘要。
3. 实现 `POST /api/runs` 手动触发。
4. 实现 `GET /api/runs` 查看最近运行。

完成标准：

- API 可以看到最近每日任务结果。
- 失败 source 不影响整体 run 完成，状态为 `partial_success`。

### Phase 4：上云部署

目标：稳定源先在云端跑起来。

任务：

1. 增加 `.env.example` 云端变量说明。
2. 增加部署文档。
3. 配置定时任务。
4. 验证秀动、摩天轮在云端可运行。
5. 大麦先标记为可选 BrowserSource。

完成标准：

- 云端每天能自动生成摘要。
- API 能从公网访问，但有 token 保护。

### Phase 5：困难源采集适配

目标：为小红书等平台建立可扩展入口。

任务：

1. 定义 `AssistedSource` 接口。
2. 允许从本机提交 raw capture 到云端。
3. 尝试 Chrome / Computer Use 搜索小红书并保存搜索结果。
4. 复用 LLM 抽取和事件去重。

完成标准：

- 至少一个困难源可以通过辅助采集进入统一摘要。
- 即使辅助采集失败，稳定源每日摘要不受影响。

### Phase 6：iOS 最小客户端

目标：移动端查看和修改订阅。

任务：

1. SwiftUI 项目初始化。
2. 配置 API base URL 和 token。
3. 今日摘要页。
4. 事件列表页。
5. 订阅设置页。

完成标准：

- iPhone 真机开发者安装后可以查看每日摘要。
- 可以修改艺人和城市，并影响下一次 worker 采集。

### Phase 7：作品化包装

目标：让项目适合作为求职 demo 展示。

任务：

1. 更新 README。
2. 增加架构图。
3. 增加 demo 截图。
4. 增加“技术取舍”章节。
5. 增加“困难源策略”章节。
6. 增加本地启动和云端部署说明。

完成标准：

- GitHub 首页能在 2 分钟内让面试官看懂项目价值。
- 代码结构能支撑一次技术面深挖。

---

## 9. Review 步骤

正式进入长期自动化 coding 前，建议按下面顺序 review。

### 9.1 产品范围 review

确认这些判断是否成立：

- 这是单人使用项目，不做多用户。
- 每日一次采集足够，不做实时。
- iOS 只负责查看摘要和修改订阅。
- 小红书等困难源允许辅助采集，不承诺纯云端全自动。
- 求职展示优先级高于“接入尽可能多的平台”。

如果以上任何一条变化，都要先改架构再写代码。

### 9.2 架构边界 review

重点看：

- API 是否只负责查询、配置、触发，而不是塞满采集细节。
- Worker 是否仍然是独立可运行的批处理任务。
- Source 是否能区分 HTTP、Browser、Assisted 三种成本模型。
- 数据库是否能支持 iOS 读取和订阅修改。

### 9.3 数据模型 review

重点看：

- `events` 是否继续作为核心事实表。
- `subscriptions` 是否足够表达当前单人需求。
- `runs` 是否能帮助排查每日任务失败。
- `daily_digests` 是否需要入库，还是先继续用 Markdown 文件。

建议第一阶段先允许 Markdown 文件和 DB 并存，不要一次性重写所有输出。

### 9.4 云端可行性 review

重点看：

- 选择哪个部署平台。
- 是否需要 Postgres。
- 定时任务由平台 cron、GitHub Actions、还是服务内 scheduler 触发。
- 云端是否需要跑浏览器。
- 如果浏览器源失败，本地辅助 worker 如何提交数据。

### 9.5 GitHub 展示 review

上传 GitHub 前至少检查：

- `.env`、`.browser-profile/`、`data/events.db`、`data/raw/` 没有入库。
- README 不暴露个人 token、webhook、cookie。
- 示例数据只保留 fixture。
- 文档说明这是个人学习项目，不用于高频抓取或商业采集。
- 架构图和路线图清楚表达工程演进。

---

## 10. 下一步建议

下一次 coding 可以从 Phase 1 开始，目标非常明确：

> 在不破坏现有本地每日任务的前提下，新增 FastAPI 服务，先把事件列表和今日摘要通过 API 暴露出来。

第一批改动建议只包含：

- `app/` 包结构
- API 入口
- 数据库查询封装
- 今日摘要读取
- 本地启动说明

不要在同一批里同时改 source、调度、iOS、部署。第一步只证明“现有本地数据可以被服务端读取并提供 API”。
