# 演出 / 活动情报监控 —— 需求与实施路线

> 一个本地部署的个人工具，持续追踪指定艺人的演唱会信息，以及上海本地有意思的展览 / 活动，抽取出开票时间、价格、购票途径，并按需通知。这是一个 vibe coding 练手项目，目标是既能跑通 MVP，又能长期迭代。
>
> 本文档自包含 —— 换电脑后打开它，按「第一步动作」和「快速启动清单」即可开工。

---

## 一、需求描述

核心用户是本人，单人使用，本地部署，所有数据存在本地文件夹里，不上云。

工具要追踪两类对象。第一类是**指定艺人的演唱会信息**：我维护一个关注的艺人名单，工具帮我盯着他们有没有新的开票动态。第二类是**上海本地有意思的展览 / 活动**：这一类没有固定名单，靠关键词和编辑类信息源去发现。

对每一条命中的记录，我关心的字段是：标题、类型（演唱会 / 展览 / 活动）、艺人（展览可空）、城市与场馆、举办日期（展览是日期**区间**）、**开票时间**、**价格**、**购票途径 / 链接**。

不需要实时。开票一般提前几天公告，每天跑一次完全够用 —— 不要被「监控」两个字带偏去做实时系统。

### 信息源分层

信息源按可靠性和抓取难度分三层，这个分层直接决定了开发顺序：

- **票务平台（大麦 / 猫眼 / 秀动）** —— 开票时间、价格、购票途径本身就是结构化数据，「购票途径」基本等于「这个源本身」。可靠性最高，但发现得晚。**MVP 从这里开始。**
- **艺人 / 主办官方号** —— 信噪比高，可作为可靠触发源，但微博这类需要登录态。
- **社交媒体关键词全站搜（小红书 / 微博）** —— 消息最早，但噪音极大、反爬最狠、需要做信息核实。**最后做，是可选模块，不是起点。**

> 关键判断：第一个项目如果一上来就啃社交媒体全站关键词监控，会把 80% 的时间花在和反爬系统搏斗上然后失去兴趣。正确顺序是先用最容易的源把整条管道打通，拿到「它真的给我发通知了」的第一个瞬间，再逐层加难的源。

---

## 二、系统本质：一条数据管道

剥到底层，这个系统不是「监控」，而是一条数据管道：

```
原始信息 → 抓取 → 结构化 → 去重/匹配 → 存储 → 通知
```

每个箭头是一个独立阶段。这个视角带来两个好处：每个阶段可以单独写、单独测；阶段之间用「存储」解耦 —— 上游把结果落盘，下游从盘里读，改下游不用重跑上游。

---

## 三、架构决策（已锁定）

对一个个人项目来说，「确定架构」不是写一份庞大设计文档，而是只锁定那几个**改起来很贵**的决策，其余全部推迟。

### 现在就锁定的（逆转成本高）

1. **核心数据模型** —— 一条「事件」记录长什么样。整个系统都流经它，定义见第五节。
2. **管道分阶段 + 原始 / 结构化切分** —— 抓取只负责把原始内容落盘，抽取从原始内容产出结构化事件。两者分两步、分开存。
3. **两个接口：`Source` 和 `Notifier`** —— 每个平台一个 `Source` 实现，每个通知渠道一个 `Notifier` 实现。原则是**接口按「将来有 N 个」设计，但 MVP 只实现 1 个**。不要写插件注册系统、抽象工厂那一套，一个老实的抽象基类 + 一个子类 + 一个配置文件就够。
4. **技术栈** —— Python（爬虫、LLM、AI 辅助资料最多）；`requests + BeautifulSoup` 处理简单页面，`Playwright` 处理需要登录态 / JS 渲染的页面；**SQLite** 做结构化存储；**用 LLM 做结构化抽取**（把抓到的原始文本丢给模型出 JSON，比写一堆易碎的 CSS selector 稳得多，源页面改版大概率不用改代码）。

### 现在不要纠结的（逆转成本低，用到再说）

具体用哪个通知渠道（文件 → 飞书 → Telegram）、调度方式（手动 → cron）、某个源用 requests 还是 Playwright、SQLite 以后换不换数据库 —— 这些都是「换一个适配器」或「加一个文件」的事，跟架构无关。

---

## 四、文件格式与目录结构

### 各类文件的格式

- **配置文件 `config.yaml`（YAML）** —— 会频繁手改（加艺人、加关键词、开关源），YAML 能写注释、最好读。
- **结构化事件 `data/events.db`（SQLite）** —— 单文件数据库，去重、「查未通知」、upsert 状态都是现成的 SQL，Python 自带 `sqlite3` 零安装。仍然是本地文件夹里的一个文件，可用任何 SQLite 浏览器打开看。
- **原始抓取内容 `data/raw/`（纯文件落盘）** —— HTML 可能很大，没理由塞进数据库，库里只存一个路径指针。文件名形如 `damai_20260515T0930_a1b2.html`。
- **每日摘要 `data/digests/`（Markdown）** —— 人直接看的东西，方便转发或同步到飞书。文件名形如 `digest_2026-05-15.md`。
- **密钥 `.env`** —— LLM API key 等，单独放，不进 `config.yaml`，写进 `.gitignore`。

### 目录结构

```
concert-monitor/
  README.md
  ARCHITECTURE.md          # 可由本文档第二、三节提炼
  requirements.txt
  .env                     # API keys（gitignore）
  .gitignore
  config.yaml              # 关注的艺人、关键词、城市、源开关
  main.py                  # 编排：遍历 sources → 抓取 → 存 raw → 抽取 → 去重 upsert → 选未通知 → notify
  db.py                    # SQLite schema + upsert / query helpers
  extractor.py             # LLM 抽取，基本与源无关
  sources/
    base.py                # Source 抽象基类
    damai.py               # MVP 唯一的源
  notifiers/
    base.py                # Notifier 抽象基类
    markdown.py            # MVP 唯一的通知
  data/
    events.db              # SQLite
    raw/                   # 原始抓取落盘
    digests/               # 每日 Markdown 摘要
```

### `config.yaml` 草样

```yaml
city: 上海
artists:
  - 周杰伦
  - 五月天
keywords:
  - 上海 展览
sources:
  damai:
    enabled: true
```

---

## 五、数据模型

### 表 `events`（结构化事件，去重后）

| 字段 | 说明 |
|---|---|
| `id` | 稳定标识，由 `类型 + 艺人/标题 + 日期 + 场馆` 哈希得到，**去重靠它** |
| `type` | `concert` / `exhibition` / `activity` |
| `title` | 标题 |
| `artist` | 艺人，展览可空 |
| `city` | 城市 |
| `venue` | 场馆 |
| `event_date` | 举办日期；展览是区间，字段要能存范围（如起止两列或文本区间） |
| `on_sale_time` | 开票时间 |
| `price_info` | 价格信息 |
| `purchase_url` | 购票途径 / 链接 |
| `source` | 哪个抓取器发现的 |
| `source_url` | 来源页面 URL |
| `raw_ref` | 指向 `data/raw/` 里对应原始文件的路径 |
| `status` | `rumored` / `confirmed` / `on_sale` —— MVP 暂时用不上，但它是未来「信息核实」功能的钩子，先留着 |
| `first_seen` | 首次发现时间 |
| `notified_at` | 通知时间，未通知为空 —— 「查未通知」靠它 |

### 表 `raw_captures`（原始抓取记录）

| 字段 | 说明 |
|---|---|
| `id` | 主键 |
| `source` | 抓取器名 |
| `url` | 抓取的页面 URL |
| `fetched_at` | 抓取时间 |
| `file_path` | `data/raw/` 下原始文件路径 |
| `processed` | 是否已被抽取过，抽取阶段只处理未处理的 |

---

## 六、第一步动作（里程碑 0：垂直切片）

**目标**：用最小代价证明「抓取 + 抽取」这条最核心的链路能通。只做一个艺人 + 一个票务平台，**不存库、不去重、不通知**。预计一到两个晚上。

具体步骤：

1. 建好第四节的目录结构，`git init`，建 Python 虚拟环境，装依赖（`requests`、`beautifulsoup4`、LLM SDK，YAML 库）。
2. 写 `config.yaml`，`artists` 里先只放一个艺人。
3. 写一个最小的 `sources/damai.py`：抓那个艺人在大麦的搜索结果页，把原始 HTML 存到 `data/raw/`。
4. 写 `extractor.py`：读那个原始文件，把文本丢给 LLM，让它按第五节 `events` 的字段吐 JSON。
5. 写 `main.py` 把上面两步串起来，`print` 出结构化结果。

**完成标准（DoD）**：跑一次 `python main.py`，终端能打印出该艺人至少一条结构化的演出记录（含开票时间 / 价格 / 购票链接字段，抓不到的字段为空也算通过）；原始 HTML 已落在 `data/raw/`。

> 纪律：里程碑 0 没跑通之前，不要碰任何「如果还能……」。范围蔓延是第一个项目的头号杀手。

---

## 七、后续 Roadmap

### 里程碑 1 —— 完整管道（单源）

加上 SQLite 存储、去重（用 `events.id` 做唯一键）、和一个真正的通知出口（先用 `notifiers/markdown.py` 输出每日摘要到 `data/digests/`）。同时落地 `raw_captures` 表和 `processed` 标志。
**DoD**：每天手动跑一次，新事件入库、重复事件不再重复通知、生成当天的 Markdown 摘要。它现在是个有用的小工具了。

### 里程碑 2 —— 多源 + 关注列表

把「源」抽象成统一接口（`sources/base.py`），新增第二、三个票务平台抓取器。`config.yaml` 的艺人列表、关键词、城市真正生效。
**DoD**：`config.yaml` 加一个艺人或一个源，无需改主流程代码即可生效。

### 里程碑 3 —— 难源 + 上海展览 / 活动

上 `Playwright` + 自己的登录 cookie，接入社交媒体和官方号（个人规模下用自己 cookie 是可接受的，不要去研究绕登录）。「上海有意思的展」天然依赖社交 / 编辑类源，在这一步落地。信息核实逻辑（社交媒体看到的 → 去票务平台对一下，更新 `status`）也在这加。
**DoD**：能从社交 / 官方源发现一条展览或活动，并标注其 `status`。

### 里程碑 4 —— 自动化 + 打磨

`cron` 定时跑；通知升级（飞书 / 邮件 / Telegram，按需选 `Notifier`）；摘要做成更易读的每日 digest；确认原始 HTML 已稳定落盘以便重跑抽取。
**DoD**：无需手动干预，每天自动产出并推送摘要。

---

## 八、要避开的坑

**先存原始数据，再抽取** —— 抓取和结构化分两步、分两张表。原始内容存下来，抽取逻辑改了能重跑而不用重新抓。

**去重从第一天就做** —— 否则通知会变成垃圾邮件。

**不需要实时** —— 每天跑一次够了，别做实时系统。

**个人用就用自己的 cookie** —— 个人规模下可接受，别去研究怎么绕登录。

**范围蔓延是第一杀手** —— 严格按里程碑推进，当前里程碑的 DoD 没达成前不要往后跳。

---

## 九、换电脑后的快速启动清单

### 准备开发环境

1. 装好 **Python（3.10+）** 和 **git**。
2. 装 **Node.js**，然后安装 terminal 版 Claude Code 作为 vibe coding 的编码 agent：`npm install -g @anthropic-ai/claude-code`。这个项目是本地部署的（本地 Python 环境、SQLite 文件、Playwright 要用本机浏览器 cookie、以后挂 cron），必须由一个跑在真实机器上的 agent 来开发——Claude Code 正是为此而生。若习惯用编辑器，可另装它的 VS Code / JetBrains 集成，引擎相同。

### 搭项目骨架

3. 新建 `concert-monitor/` 文件夹，`git init`，按第四节建好目录结构。
4. 建虚拟环境并安装依赖，写好 `requirements.txt` 和 `.gitignore`（至少忽略 `.env`、`data/`、虚拟环境目录）。
5. 把本文档第二、三节提炼进 `ARCHITECTURE.md`，作为项目内的架构参照。
6. 配好 `.env`（LLM API key）和 `config.yaml`（先放一个艺人）。

### 开始开发

7. 在项目文件夹里运行 `claude` 进入 Claude Code，把本文档一并交给它作为上下文。
8. 按第六节「第一步动作」逐步实现，直到里程碑 0 的 DoD 通过。
9. 之后按第七节 Roadmap 逐个里程碑推进。每个里程碑都要自己在 DoD 上把关，看清楚 agent 到底改了什么——vibe coding 不等于撒手。

> 给 Claude Code 的第一条提示可以是：「按这份文档第六节，搭好目录骨架，并实现里程碑 0 的垂直切片 —— 一个大麦抓取器 + 一个 LLM 抽取器 + 串起来的 main.py，跑一次能 print 出指定艺人的结构化演出记录。」
